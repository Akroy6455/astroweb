const { swisseph } = require('@fusionstrings/swiss-eph');

async function test() {
  // It might be a class or an async init?
  console.log(Object.keys(require('@fusionstrings/swiss-eph')));
}
test();
